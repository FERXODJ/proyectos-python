# proyectos-python

# Crear el Entorno Virtual (python -m venv venv)

# Activar el Entorno Virtual (source venv/Scripts/activate)

# Instalar las dependencias (pip install -r requirements.txt)

# Importar Base de datos (pip install flask-sqlalchemy)

# Instalar Flask (pip install flask)

# Importar Flask-CORS (pip install flask-cors)

# Correr ProyectO (flask run)


